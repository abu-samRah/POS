import React from 'react'
import ButtonElement, { ButtonProps } from '@material-ui/core/Button';



const Button: React.FC<ButtonProps> = (props) => {
  return <Button {...props} />;
}

export default ButtonElement
